# Copy to backend.hcl (not committed) and fill in your S3 backend details.
# Then: terraform init -backend-config=backend.hcl

bucket = "<your-terraform-state-bucket>"
key    = "<your-state-key>"
region = "<your-aws-region>"
